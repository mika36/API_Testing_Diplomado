def urlInText(text):
    size = len(text)
    initialPosition = text.find("http://")
    print(initialPosition)
    currentPosition = initialPosition
    while(currentPosition )
    endPosition


   #else:
    #    print("no existe una URL en la cadena")

urlInText("12http://")